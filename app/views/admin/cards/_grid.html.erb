<div class="tab-content">
  <div class="tab-pane active" id="index">
    <%= table_for @card.table_body, @card.table_head do |row| %>
    <tr>
      <% row.each do |value| %>
        <td><%= value %></td>
      <% end %>
    </tr>
    <% end %>
  </div>
</div>
